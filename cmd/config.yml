# Linkup API – config file (optional).
# Defaults are defined in main; uncomment to override.
# Env overrides: LINKUP_SERVER_ADDRESS, LINKUP_DATABASE_URL, LINKUP_JWT_SECRET, LINKUP_CORS_ORIGINS, LINKUP_AUTH_CODE_TTL, LINKUP_AUTH_CODE_RATE_LIMIT_*, LINKUP_EMAIL_PROVIDER

server:
  # address: ":8181"

database:
  # url: "linkup.db"

jwt:
  # secret: "dev-secret"

cors:
  # origins: "http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:5173"

auth:
  # code_ttl: "10m"
  # code_rate_limit_max: 3      # max requests per email per window (0 = disable)
  # code_rate_limit_window: "15m"

email:
  # provider: "stub"           # stub | resend | sendgrid (future)
